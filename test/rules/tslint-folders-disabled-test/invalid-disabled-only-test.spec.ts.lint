describe("someTestSuite - OK", () => {
    it("someTest - OK", () => {
        // test code here
    });

    it.only("someTest - only", () => {
    ~~ [do not enable only some tests (tslint-folders-disabled-test)]
        // test code here
    });
});

describe.only("someTestSuite - only", () => {
~~~~~~~~ [do not enable only some tests (tslint-folders-disabled-test)]
    it("someTest - OK", () => {
        // test code here
    });

    it.only("someTest - only", () => {
    ~~ [do not enable only some tests (tslint-folders-disabled-test)]
        // test code here
    });

    // test inside a loop
    [1,2].forEach(value => {
        describe.only("someTestSuite - only", () => {
        ~~~~~~~~ [do not enable only some tests (tslint-folders-disabled-test)]
            it("someTest - OK", () => {
                // test code here
            });

            it.only("someTest - only", () => {
            ~~ [do not enable only some tests (tslint-folders-disabled-test)]
                // test code here
            });
        });
    });
});
